linters:
  csslint: {  }
  checkstyle:
    config: './google_checks.xml'
  eslint:
    config: './eslint.config'
files:
  ignore:
    - 'bower_components/*'
    - 'node_modules/*'
fixers:
  enable: true
